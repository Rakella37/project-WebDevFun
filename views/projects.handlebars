<div class="principal">
    <h1 class="proj-name">Projects</h1>
    <ul class="project-container">
        {{#each projects}}
            <li class="project">
                <h2 class="project-title">{{this.ptitle}}</h2> <!-- Use this.ptitle to access the title -->
                <p class="project-date">{{this.pdate}}</p> <!-- Use this.date to access the date -->
                {{#if this.pimage}}
                <a href="/projects/{{this.pid}}">
                    <img src="{{this.pimage}}" alt="{{this.ptitle}}">
                </a>
                {{/if}}  
            </li>
        {{/each}}
    </ul>
</div>


