   <div id="skills">
  <h1 class="skills-title">Skills</h1>
  <ul>
    {{#each skills}}
      <li class="skill-item">
        <!-- Displaying the skill title and description -->
        <h3 class="skill-topic">{{this.stitle}}</h3>
        <p class="p-space">{{this.sdescription}}</p>

        <!-- If the user is an admin, show the update and delete forms -->
        {{#if ../session.isAdmin}}
        <!-- Form to update skill title and description -->
          <form action="/skills/update/{{this.sid}}" method="post" class="skills-form">
             <!-- Input for the new skill title -->
            
            <input class="skills-inputs" type="text" name="stitle" placeholder="New Title" required />
             <!-- Input for the new skill description -->
            <input
            class="skills-inputs"
              type="text"
              name="sdescription"
              placeholder="New Description"
              required
            />
              <!-- Submit button for updating the skill -->
            <button class="skill-btn" type="submit">Update</button>
          </form>

        <!-- Form to delete a skill -->
          <form
            action="/skills/delete/{{this.sid}}"
            method="post"
            style="display:inline;"
          >
          <!-- Submit button for deleting the skill, with a confirmation message pop up  -->
            <button
            class="skill-btn-delete" 
              type="submit"
              onclick="return confirm('Are you sure you want to delete this skill?');"
            >Delete</button>
          </form>
        {{/if}}
      </li>
    {{/each}}
  </ul>

<!-- If the user is an admin, show the form to add new skills -->
  {{#if session.isAdmin}}
    <h2 class="add-skills-title">Add New Skill</h2>
    <form action="/skills/add" method="post">
      <input class="add-skills-input" type="text" name="stitle" placeholder="Skill Title" required />
      <input
      class="add-skills-input"
        type="text"
        name="sdescription"
        placeholder="Skill Description"
        required
      />
       <!-- Submit button for adding the new skill -->
      <button class="skills-btn" type="submit">Add Skill</button>
    </form>
  {{/if}}
</div>